# GBMVax — Python dependencies.
# Pinned to versions tested on both MPS (Mac M-series, primary dev) and CUDA
# (Colab/lab GPU box, training). torch >= 2.1 is required for stable MPS
# support of nn.Transformer operations.

# Core ML
torch>=2.1.0
accelerate>=0.25.0          # Device-agnostic training loop (MPS/CUDA/CPU)
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Bioinformatics
biopython>=1.81             # FASTA parsing, BLOSUM62 matrices, alignment
pyyaml>=6.0

# Validation / clinical-trial supplements come as Excel
openpyxl>=3.1.0
xlrd>=2.0.1

# Logging / tracking
tqdm>=4.66.0
wandb>=0.16.0               # Optional — disabled if no API key

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
